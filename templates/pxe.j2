DEFAULT pxeboot
TIMEOUT 20
PROMPT 0
LABEL pxeboot 
	kernel coreos/{{ okd.kernel_file }}
	append initrd=coreos/{{ okd.initramfs_file }},coreos/{{ okd.rootfs_file }} coreos.inst.install_dev={{ disk }} coreos.inst.ignition_url=http://{{ infra.ipaddr }}:{{ infra.http_port }}/ignition/{{ item.role }}.ign

